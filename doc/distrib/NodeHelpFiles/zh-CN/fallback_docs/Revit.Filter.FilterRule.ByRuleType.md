## 详细
“FilterRule.ByRuleType”基于选定的规则类型、参数和比较值创建 Revit 过滤规则。

在下面的示例中，将标识墙类型，并将“防火等级”参数与值和规则类型一起用作“FilterRule.ByRuleType”的输入。输出是过滤规则，然后将其应用于视图。
___
## 示例文件

![FilterRule.ByRuleType](./Revit.Filter.FilterRule.ByRuleType_img.jpg)
