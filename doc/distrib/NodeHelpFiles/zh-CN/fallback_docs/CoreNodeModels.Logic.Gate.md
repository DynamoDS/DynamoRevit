## 详细
“Gate”根据布尔值控制是否允许输入数据通过。如果浇口打开(True)，则输入将传递到输出。如果浇口关闭(False)，则阻止输入。

在下面的示例中，“Gate”用于防止几何图形在每次图形运行时被分解。当浇口打开(True)时，新创建的几何图形将通过并分解。当浇口关闭(False)时，将阻止几何图形，且不会传递或再次分解。
___
## 示例文件

![Gate](./CoreNodeModels.Logic.Gate_img.jpg)
