## 详细
此节点将检索与给定 ScheduleFilter 关联的 ScheduleFieldId。Revit 中的每个明细表过滤器都链接到特定字段(例如，标高、族和类型、面积)。此节点通过返回过滤器使用的字段标识符来公开该连接。

在此示例中，将创建明细表过滤器并将其用作 ScheduleFilter.FileId 的输入。
___
## 示例文件

![ScheduleFilter.FiledId](./Revit.Schedules.ScheduleFilter.FiledId_img.jpg)
