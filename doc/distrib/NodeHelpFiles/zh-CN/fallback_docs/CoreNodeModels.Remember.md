## 详细
“Remember”将值存储在 Dynamo 文件中，以便以后图形可以调用该值，包括当输入为空时。它可用于在运行之间保留数据、维护状态或引用以前存储的信息，无需每次都重新创建。

在下面的示例中，选择单个楼板，提取几何图形并将其用作“Remember”的输入。然后，存储的结果将用作输出，以便在图表中重用。
___
## 示例文件

![Remember](./CoreNodeModels.Remember_img.jpg)
