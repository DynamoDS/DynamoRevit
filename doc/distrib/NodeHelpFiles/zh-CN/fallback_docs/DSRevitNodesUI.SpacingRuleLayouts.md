## 详细
“Spacing Rule Layout”从分割路径元素中检索间距规则布局。分割路径用作输入，输出是控制点如何沿路径分布的当前间距布局规则。

在下面的示例中，从 Revit 模型中选择分割表面，并用作输入以修改其参数。字符串节点提供参数名称“Layout”，而“Spacing Rule Layout”提供间距规则类型值。图表会将分割表面布局规则更新为“FixedNumber”。输出是修改后的分割表面单元。
___
## 示例文件

![Spacing Rule Layout](./DSRevitNodesUI.SpacingRuleLayouts_img.jpg)
