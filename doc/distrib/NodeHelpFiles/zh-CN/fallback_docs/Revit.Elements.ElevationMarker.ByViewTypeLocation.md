## 详细
此节点通过指定模型中的“视图类型”和位置点，在活动的 Revit 文档中创建新的立面标记。

在此示例中，将在将放置立面的所需位置创建一个点。此点与指定的视图类型族和比例因子一起作为 ElevationMarker.ByViewTypeLocation 节点的位置输入提供。然后，生成的 ElevationMarker 用于按定义的比例生成立面视图。
___
## 示例文件

![ElevationMarker.ByViewTypeLocation](./Revit.Elements.ElevationMarker.ByViewTypeLocation_img.jpg)
