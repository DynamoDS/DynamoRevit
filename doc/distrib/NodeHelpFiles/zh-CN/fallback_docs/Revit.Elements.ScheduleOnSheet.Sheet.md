## 详细
此节点获取明细表所在的图纸。

在此示例中，明细表放置在图纸上的指定位置。然后，将明细表输入到 ScheduleOnSheet.Sheet 节点，其中会显示图纸名称(名称 = 封面)。最后，将名称提取到字符串中。

___
## 示例文件

![ScheduleOnSheet.Sheet](./Revit.Elements.ScheduleOnSheet.Sheet_img.jpg)
