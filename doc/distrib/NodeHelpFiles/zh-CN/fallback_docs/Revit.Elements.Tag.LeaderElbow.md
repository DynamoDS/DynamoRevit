## 详细
“Tag.LeaderElbow”返回标记的引线的弯曲点。输入是 Revit 标记图元，输出是表示引线弯头位置的 Dynamo 点。

在下面的示例中，从活动 Revit 视图中选择一个标记，并将其用作“Tag.LeaderElbow”的输入。输出是每个标记引线弯曲的点位置。
___
## 示例文件

![Tag.LeaderElbow](./Revit.Elements.Tag.LeaderElbow_img.jpg)
