## 详细
“Quantities”提供可选的数量类型(如长度、面积或体积)并提取关联的单位定义。这些数量代表所描述的测量类型，而不是实际单位值。例如，长度是数量，而英尺或米是属于该数量的单位。

在下面的示例中，“Quantities”用于选择一个值(例如长度、面积或体积)并提取关联的单位定义。
___
## 示例文件

![Quantities](./UnitsUI.Quantities_img.jpg)
