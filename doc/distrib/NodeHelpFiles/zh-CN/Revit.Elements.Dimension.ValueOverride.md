## 详细
“Dimension.ValueOverride”将返回给定尺寸标注的值替代(如果该标注具有替代值)。对于多段尺寸标注，将返回值的嵌套列表。如果尺寸标注没有替代值，将返回空值。

在下面的示例中，将从活动视图中收集第一个尺寸标注，并检查该尺寸标注是否具有替代尺寸标注。如果该尺寸标注被替代，则使用“Dimension.SetValueOverride”清除它。
___
## 示例文件

![Dimension.ValueOverride](./Revit.Elements.Dimension.ValueOverride_img.jpg)
