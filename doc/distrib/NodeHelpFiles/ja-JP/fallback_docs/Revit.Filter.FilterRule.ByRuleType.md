## 詳細
`FilterRule.ByRuleType` は、選択した規則タイプ、パラメータ、および比較値に基づいて Revit のフィルタ規則を作成します。

次の例では、壁タイプを識別し、`Fire Rating` のパラメータを値および規則タイプとともに `FilterRule.ByRuleType` への入力として使用しています。出力はフィルタ規則で、その後ビューに適用されます。
___
## サンプル ファイル

![FilterRule.ByRuleType](./Revit.Filter.FilterRule.ByRuleType_img.jpg)
