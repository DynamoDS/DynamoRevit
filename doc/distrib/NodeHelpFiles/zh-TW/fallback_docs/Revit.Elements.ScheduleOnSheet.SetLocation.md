## 深入資訊
此節點會重置明細表在圖紙上的位置。

在此範例中，將明細表放在圖紙上，然後輸入到 SheduleOnSheet.SetLocation 節點，同時提供新的一點。明細表會在同一張圖紙上重新定位。最後一個節點會取得明細表的新位置。
___
## 範例檔案

![ScheduleOnSheet.SetLocation](./Revit.Elements.ScheduleOnSheet.SetLocation_img.jpg)
