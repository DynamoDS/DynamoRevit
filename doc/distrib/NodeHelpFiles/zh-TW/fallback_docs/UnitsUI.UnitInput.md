## 深入資訊
`Parse Unit Input` 接受數值搭配單位類型，提供一種定義數量 (例如長度、面積、體積、角度或其他受控格式的可測量值) 的方式。

在以下範例中，使用 `Parse Unit Input` 搭配值 10 和「Feet」單位。輸出是值為 10 和單位名稱為「Feet」的 `System.Double`。
___
## 範例檔案

![Parse Unit Input](./UnitsUI.UnitInput_img.jpg)
