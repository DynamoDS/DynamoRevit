## 深入資訊
`Units` 提供當其他節點需要 `DynamoUnits.Unit` 輸入時可使用的單位類型清單。輸出包括如長度、面積、體積、角度，以及其他支援類型 (視 Dynamo 版本而定) 的單位類型。

在以下範例中，`Units` 會產生 Dynamo 單位物件的清單。然後可以選取或篩選這些單位，並傳入需要 `DynamoUnits.Unit` 輸入的節點。
___
## 範例檔案

![Units](./UnitsUI.Units_img.jpg)
