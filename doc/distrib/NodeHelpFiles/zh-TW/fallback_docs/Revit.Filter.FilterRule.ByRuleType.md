## 深入資訊
`FilterRule.ByRuleType` 會根據選取的規則類型、參數和比較值，建立 Revit 篩選規則。

以下範例識別牆類型，使用 `Fire Rating` 參數作為 `FilterRule.ByRuleType` 的輸入，同時提供值和規則類型。輸出是之後要套用到視圖的篩選規則。
___
## 範例檔案

![FilterRule.ByRuleType](./Revit.Filter.FilterRule.ByRuleType_img.jpg)
