## 深入資訊
此節點會沿著定義的曲線，在 Revit 模型中建立結構樑。由曲線、選取的構架類型和關聯的樓層來定義樑。輸出是建立的結構樑元素。

在此範例中，於 Snowdon 結構模型中樓層 L2 TOS 的網格 A5 和 A6 之間放置一條線。提供這條線作為曲線輸入，選取的構架類型和樓層定義樑。輸出是新建立的結構樑元素。
___
## 範例檔案

![StructuralFraming.BeamByCurve](./Revit.Elements.StructuralFraming.BeamByCurve_img.jpg)
