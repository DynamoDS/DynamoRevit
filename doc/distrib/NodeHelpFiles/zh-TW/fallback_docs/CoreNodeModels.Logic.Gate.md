## 深入資訊
`Gate` 控制是否根據布林值允許輸入資料通過。如果閘門開啟 (True)，輸入可傳遞到輸出。如果閘門關閉 (False)，輸入會被阻擋。

在以下範例中，使用 `Gate` 防止幾何圖形在每次執行圖表時被分解。當閘門開啟 (True) 時，新建立的幾何圖形會通過並分解。當閘門關閉 (False) 時，幾何圖形會被阻擋，不會再通過或分解。
___
## 範例檔案

![Gate](./CoreNodeModels.Logic.Gate_img.jpg)
