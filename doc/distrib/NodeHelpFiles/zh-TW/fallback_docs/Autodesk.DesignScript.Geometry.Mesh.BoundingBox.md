## 深入資訊
`Mesh.BoundingBox` 會傳回給定網格的邊界框。

以下範例產生點並建立網格，然後使用該網格作為 `Mesh.BoundingBox` 的輸入。輸出是用來擷取 `MinPoint` 的邊界框。
___
## 範例檔案

![Mesh.BoundingBox](./Autodesk.DesignScript.Geometry.Mesh.BoundingBox_img.jpg)
