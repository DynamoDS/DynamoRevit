## 深入資訊
`Symbols` 提供一個可選取的單位符號清單，Dynamo 在格式化或剖析有單位的值時可使用這些單位符號。

在以下範例中，使用 `Symbols` 從 Dynamo 可用的單位符號資源庫中選取單位符號。輸出是 `DynamoUnits.Symbol` 物件，可作為格式化分數值時的輸入。
___
## 範例檔案

![Symbols](./UnitsUI.Symbols_img.jpg)
