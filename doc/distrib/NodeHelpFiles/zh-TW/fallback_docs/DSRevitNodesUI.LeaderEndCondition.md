## 深入資訊
`Leader End Condition` 提供可從 Revit 中選取的引線終點條件值。選項為「自由」或「貼附」。

在以下範例中，視圖 L1 中的所有牆都加上標籤。使用這些標籤，搭配從 `Leader End Condition` 選取的「自由」值，作為設定引線終止條件值的輸入。
___
## 範例檔案

![LeaderEnd Condition](./DSRevitNodesUI.LeaderEndCondition_img.jpg)
