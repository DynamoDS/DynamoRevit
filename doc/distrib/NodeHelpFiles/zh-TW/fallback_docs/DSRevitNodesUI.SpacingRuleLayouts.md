## 深入資訊
`Spacing Rule Layout` 會從分割的路徑元素擷取間距規則配置。使用分割的路徑作為輸入，輸出是目前的間距配置規則，控制這些點如何沿著路徑分佈。

以下範例從 Revit 模型中選取一個分割的表面，作為修改其參數的輸入。字串節點提供參數名稱「配置」，`Spacing Rule Layout` 提供間距規則類型值。圖表會將分割表面配置規則更新為 `FixedNumber`。輸出是修改後的分割表面元素。
___
## 範例檔案

![Spacing Rule Layout](./DSRevitNodesUI.SpacingRuleLayouts_img.jpg)
