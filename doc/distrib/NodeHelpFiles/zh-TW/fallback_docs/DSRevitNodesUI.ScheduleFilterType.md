## 深入資訊
此節點有一個允許的明細表篩選類型的下拉式清單 (Equal、NotEqual、GreaterThan、LessThan、Contains...）。結果會篩選所有以「Exterior」為開頭的門。

在此範例中，選取視圖並顯示視圖欄位。「Type」欄位是獨立的，用於建立明細表篩選，同時提供篩選值和「BeginsWith」明細表篩選類型，然後將此篩選套用到選取的明細表視圖「WV_Wall Schedule」。
___
## 範例檔案

![Schedule Filter Type](./DSRevitNodesUI.ScheduleFilterType_img.jpg)
