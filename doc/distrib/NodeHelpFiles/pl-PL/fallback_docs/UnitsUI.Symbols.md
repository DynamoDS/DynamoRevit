## Informacje szczegółowe
`Symbols` provides a selectable list of unit symbols that Dynamo can use when formatting or parsing unit-based values.

In the example below, `Symbols` is used to select a unit symbol from Dynamo's available unit symbol library. The output is a `DynamoUnits.Symbol` object that can be used as an input when formatting fractional values.
___
## Plik przykładowy

![Symbols](./UnitsUI.Symbols_img.jpg)
